export const enum paths {
  admin = '/admin',
  login = '/login',
  register = '/register',
  user = '/user',
  main = '/',
  room = '/room',
  building = '/buildings',
  service = '/services',
  type = '/types',
}

export const enum roles {
  admin = 'admin',
  client = 'client',
}
